"""baglab — data review pipeline for G1 robot rosbags (medallion architecture).

Bronze tier: automated quality checks on raw bags (data integrity, trajectory
smoothness, static-frame scrubbing). See baglab.bronze.
"""

__version__ = "0.1.0"
